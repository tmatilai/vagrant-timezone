en:
  vagrant_timezone:
    not_enabled: "Time zone not configured"
    not_supported: "Setting time zone not supported"
    configuring: "Setting time zone..."

    errors:
      vagrant_version: |-
        vagrant-timezone plugin requires Vagrant version %{requirement}
